# Отчет об оптимизации проекта bizon-server

## 🔴 Критические проблемы производительности

### 1. N+1 Query Problem в `/api/orders`
**Проблема:** В эндпоинте `/api/orders` (строки 755-793) выполняется один запрос для получения заказов, а затем для каждого заказа отдельный запрос для получения товаров. Это классическая проблема N+1.

**Текущий код:**
```javascript
const ordersWithItems = await Promise.all(ordersResult.rows.map(async (order) => {
  const itemsResult = await pool.query(`
    SELECT ... FROM order_items WHERE order_id = $1
  `, [order.id]);
  return { ...order, items: itemsResult.rows || [] };
}));
```

**Решение:** Использовать один запрос с JOIN:
```javascript
const ordersWithItemsResult = await pool.query(`
  SELECT 
    o.id, o.phone, o.comment, o.total_amount, o.created_at, 
    COALESCE(o.status, 'новый') as status,
    oi.product_id, oi.product_title, oi.quantity, 
    oi.price_per_unit, (oi.quantity * oi.price_per_unit) as total_price
  FROM orders o
  LEFT JOIN order_items oi ON o.id = oi.order_id
  ORDER BY o.created_at DESC, oi.id
`);
// Затем группировать результаты по order_id
```

**Ожидаемый эффект:** Сокращение количества запросов с N+1 до 1, значительное ускорение при большом количестве заказов.

---

### 2. Дублирование логики парсинга изображений
**Проблема:** Логика парсинга `images_json` повторяется минимум 5 раз в разных местах (строки 146-164, 393-409, 458-476, 1900-1920, 2995-3012).

**Решение:** Создать утилитарную функцию:
```javascript
function parseImagesJson(imagesJson, productId = null) {
  if (!imagesJson) return [];
  
  if (typeof imagesJson === 'string') {
    try {
      const parsed = JSON.parse(imagesJson);
      return Array.isArray(parsed) ? parsed : [];
    } catch (e) {
      console.error(`Ошибка парсинга images_json для товара ${productId}:`, e);
      return [];
    }
  } else if (Array.isArray(imagesJson)) {
    return imagesJson;
  } else if (typeof imagesJson === 'object') {
    return [imagesJson];
  }
  return [];
}
```

**Ожидаемый эффект:** Уменьшение дублирования кода, упрощение поддержки, единообразная обработка ошибок.

---

## 🟡 Важные оптимизации

### 3. Отсутствие кэширования
**Проблема:** Часто запрашиваемые данные (товары, категории, аттракционы) загружаются из БД при каждом запросе.

**Решение:** Добавить кэширование с использованием `node-cache` или `redis`:
```javascript
const NodeCache = require('node-cache');
const cache = new NodeCache({ stdTTL: 300 }); // 5 минут

app.get('/api/products', async (req, res) => {
  const cacheKey = `products_${req.query.admin}_${req.query.show_all}`;
  const cached = cache.get(cacheKey);
  if (cached) return res.json(cached);
  
  // ... выполнение запроса ...
  cache.set(cacheKey, products);
  res.json(products);
});
```

**Ожидаемый эффект:** Снижение нагрузки на БД, ускорение ответов API.

---

### 4. Дублирование функций в `cart.js`
**Проблема:** Функция `renderCartItems` определена дважды (строки 16-76 и 233-340), `updateSendOrderButton` также дублируется.

**Решение:** Удалить дублирующиеся определения, оставить только одну версию каждой функции.

---

### 5. Большой файл `server.js` (3210 строк)
**Проблема:** Весь код сервера в одном файле, что затрудняет поддержку и тестирование.

**Решение:** Разделить на модули:
- `routes/products.js` - маршруты для товаров
- `routes/orders.js` - маршруты для заказов
- `routes/attractions.js` - маршруты для аттракционов
- `utils/db.js` - утилиты для работы с БД
- `utils/images.js` - утилиты для работы с изображениями
- `middleware/auth.js` - middleware для аутентификации

---

### 6. Неоптимальная конфигурация пула подключений
**Проблема:** Используются дефолтные настройки пула PostgreSQL.

**Решение:** Настроить пул под нагрузку:
```javascript
const pool = new Pool({
  connectionString: ...,
  max: 20, // максимальное количество клиентов в пуле
  idleTimeoutMillis: 30000,
  connectionTimeoutMillis: 2000,
  // ...
});
```

---

### 7. Отсутствие индексов в БД
**Рекомендация:** Проверить наличие индексов на часто используемых полях:
- `products.slug` - для поиска по slug
- `products.available` - для фильтрации доступных товаров
- `products.category` - для фильтрации по категориям
- `order_items.order_id` - для JOIN запросов
- `product_variants_link.product_id` и `product_variants_link.group_id`

---

## 🟢 Дополнительные улучшения

### 8. Добавить compression middleware
```javascript
const compression = require('compression');
app.use(compression());
```
Сократит размер ответов и ускорит передачу данных.

### 9. Добавить rate limiting
Защита от злоупотреблений API:
```javascript
const rateLimit = require('express-rate-limit');
const limiter = rateLimit({
  windowMs: 15 * 60 * 1000, // 15 минут
  max: 100 // максимум 100 запросов
});
app.use('/api/', limiter);
```

### 10. Оптимизация Puppeteer
**Проблема:** При каждом запросе PDF создается новый браузер (строка 3113).

**Решение:** Использовать пул браузеров или переиспользовать один экземпляр:
```javascript
let browserInstance = null;
async function getBrowser() {
  if (!browserInstance) {
    browserInstance = await puppeteer.launch({...});
  }
  return browserInstance;
}
```

### 11. Валидация входных данных
Добавить библиотеку `joi` или `express-validator` для валидации запросов.

### 12. Логирование
Использовать структурированное логирование (например, `winston` или `pino`) вместо `console.log`.

### 13. Обработка ошибок
Создать централизованный обработчик ошибок вместо дублирования try-catch блоков.

---

## 📊 Приоритеты внедрения

1. **Высокий приоритет:**
   - N+1 Query в `/api/orders` (критично для производительности)
   - Вынести функцию парсинга изображений (упростит поддержку)
   - Удалить дублирующиеся функции в `cart.js`

2. **Средний приоритет:**
   - Добавить кэширование
   - Разделить `server.js` на модули
   - Оптимизировать конфигурацию пула БД

3. **Низкий приоритет:**
   - Compression middleware
   - Rate limiting
   - Структурированное логирование

---

## 🛠️ Рекомендуемые зависимости

```json
{
  "compression": "^1.7.4",
  "express-rate-limit": "^7.1.5",
  "node-cache": "^5.1.2",
  "winston": "^3.11.0",
  "joi": "^17.11.0"
}
```

